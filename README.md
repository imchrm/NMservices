# NoMus Services (NMservices)

Это репозиторий бэкенд-сервисов для проекта **NoMus Marketplace**. Сервис предоставляет API для клиентских приложений, основным из которых является Telegram-бот [`@nomus_uz_bot`](https://t.me/nomus_uz_bot) (предполагается).

Проект реализован в рамках **Proof of Concept (PoC)** для проверки архитектурных гипотез и основных бизнес-сценариев.

## 🚀 Архитектура

Проект построен на принципах **клиент-серверной архитектуры**:

*   **Клиент (Фронтенд):** Telegram-бот, который отвечает за взаимодействие с пользователем (UI/UX). Его логика и структура описаны в документации (`doc/bot_flow.md`, `doc/sop.md`).
*   **Сервер (Бэкенд):** Данный проект. Он реализован на **FastAPI** и отвечает за бизнес-логику, обработку данных и интеграции с внешними системами (даже если на этапе PoC они представлены заглушками).

Такое разделение обеспечивает:
*   **Масштабируемость:** К этому API в будущем можно будет подключить мобильное приложение или веб-сайт.
*   **Безопасность:** Вся чувствительная логика (платежи, работа с БД) инкапсулирована на сервере.
*   **Независимость развертывания:** Можно обновлять логику на сервере, не затрагивая и не перезапуская бота.

Структура проекта следует принципам **Чистой архитектуры** (Clean Architecture), разделяя код на слои: `Domain`, `Application`, `Infrastructure` и `Presentation`.

## 🛠️ Технологический стек

*   **Язык:** Python 3.11+
*   **Фреймворк:** FastAPI
*   **Сервер:** Uvicorn
*   **Управление зависимостями:** Poetry
*   **Линтинг и форматирование:** Ruff
*   **Тестирование:** Pytest

## API Endpoints (PoC)

На этапе PoC сервис предоставляет следующие основные эндпоинты:

*   `GET /docs`: Автоматически сгенерированная интерактивная документация API (Swagger UI).
*   `POST /register`: Регистрация пользователя. Принимает данные от бота (например, номер телефона) и имитирует сохранение в базу данных.
*   `POST /create_order`: Создание заказа. Принимает ID пользователя и тариф, имитирует процесс оплаты и сохранение заказа.

## ⚙️ Установка и запуск

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/imchrm/NMservices.git
    cd NMservices
    ```

2.  **Install Poetry:**

Official installation [guide](https://python-poetry.org/docs/#installation).


3.  **Установите зависимости:**
    Убедитесь, что у вас установлен Poetry.
    ```bash
    poetry install
    ```

4.  **Запустите сервис:**
    Для запуска веб-сервиса в режиме разработки (с автоматической перезагрузкой) выполните:
    ```bash
    poetry run uvicorn nms.main:app --reload --app-dir src
    ```
    *(Путь `src.nomus.main:app` основан на структуре из `doc/sop.md`. Если точка входа отличается, скорректируйте команду)*

5.  **Проверьте работу API:**
    После запуска откройте в браузере адрес http://127.0.0.1:8000/docs.
    Вы увидите интерфейс Swagger, где можно протестировать эндпоинты `/register` и `/create_order` напрямую, отправляя JSON-запросы.

## ➡️ Следующие шаги

*   Реализация логики в слоях `Application` и `Infrastructure` согласно `sop.md`.
*   Настройка взаимодействия между ботом и этим API с помощью HTTP-запросов (например, через библиотеку `httpx`).
*   Написание тестов с использованием `pytest`.
