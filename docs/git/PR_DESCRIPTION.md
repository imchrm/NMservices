# Pull Request: Refactor project structure with Clean Architecture

## 📋 Описание изменений

Полный рефакторинг структуры проекта с применением принципов Clean Architecture. Код стал более модульным, тестируемым и масштабируемым.

## 🎯 Цели

- Разделить код на чёткие слои ответственности
- Упростить добавление новых фичей
- Улучшить тестируемость кода
- Централизовать конфигурацию
- Сохранить обратную совместимость

## 🏗️ Что было сделано

### 1. Архитектура (Clean Architecture)
```
src/nms/
├── api/          # Presentation Layer (роутеры)
├── models/       # Data Models (Pydantic)
├── services/     # Business Logic Layer
├── config.py     # Configuration
└── main.py       # Application Entry Point
```

### 2. Конфигурация
- ✅ Централизованный модуль `config.py` с `pydantic-settings`
- ✅ Поддержка `.env` файлов
- ✅ Добавлен `.env.example` шаблон
- ✅ Кеширование настроек через `@lru_cache`

### 3. Модели данных
- ✅ Разделены по доменам: `models/user.py`, `models/order.py`
- ✅ Улучшена документация с помощью `Field` descriptions
- ✅ Централизованный `__init__.py` для импортов

### 4. Сервисный слой
- ✅ `AuthService` - аутентификация и регистрация (`services/auth.py`)
- ✅ `OrderService` - управление заказами (`services/order.py`)
- ✅ Бизнес-логика вынесена из эндпоинтов

### 5. API роутеры
- ✅ `api/users.py` - роутер пользователей (префикс `/users`)
- ✅ `api/orders.py` - роутер заказов (префикс `/orders`)
- ✅ `api/dependencies.py` - общие зависимости (auth middleware)

### 6. Главный модуль
- ✅ `main.py` очищен от бизнес-логики
- ✅ Подключены роутеры
- ✅ Legacy эндпоинты помечены как `deprecated`

### 7. Инфраструктура
- ✅ Обновлен `.gitignore` (добавлена `.claude/`)
- ✅ Добавлена зависимость `pydantic-settings`
- ✅ Все тесты обновлены и проходят

## 🆕 Новые эндпоинты

| Старый (deprecated) | Новый (рекомендуется) |
|-------------------|---------------------|
| `POST /register` | `POST /users/register` |
| `POST /create_order` | `POST /orders` |

## ✅ Обратная совместимость

- Все старые эндпоинты продолжают работать
- API контракты не изменились
- Существующие клиенты не затронуты

## 🧪 Тестирование

```bash
poetry run pytest -v
```

**Результат:**
```
====== 6 passed in 1.46s ======
```

Все тесты проходят без ошибок и предупреждений.

## 📊 Статистика

- **Коммитов:** 4
- **Файлов изменено:** 18
- **Добавлено строк:** +516
- **Удалено строк:** -108

## 🎁 Преимущества новой структуры

### Для разработки
1. **Separation of Concerns** - каждый модуль отвечает за свою задачу
2. **Масштабируемость** - легко добавлять новые домены и эндпоинты
3. **Тестируемость** - сервисы можно тестировать независимо от API
4. **Читаемость** - понятная структура и навигация по коду

### Для поддержки
5. **Централизованная конфигурация** - все настройки в одном месте
6. **Модульность** - легко найти нужный код
7. **Документированность** - docstrings и type hints везде

### Для масштабирования
8. **Расширяемость** - новые фичи не ломают старый код
9. **Независимость слоёв** - можно менять implementation без изменения API
10. **Clean Architecture** - готовность к росту проекта

## 📝 Миграционный план

### Фаза 1: Merge (сейчас)
- Смержить `strange-lederberg` в `main`
- Запустить на production
- Мониторить работу legacy эндпоинтов

### Фаза 2: Миграция клиентов (следующий спринт)
- Обновить Telegram бота для использования новых эндпоинтов
- Обновить документацию API
- Уведомить других разработчиков

### Фаза 3: Удаление legacy (через месяц)
- Удалить deprecated эндпоинты из `main.py`
- Очистить unused imports

## 🔍 Как проверить

### 1. Установить зависимости
```bash
poetry install
```

### 2. Запустить тесты
```bash
poetry run pytest -v
```

### 3. Запустить сервер
```bash
poetry run nms
```

### 4. Проверить API
Откройте http://127.0.0.1:8000/docs

Проверьте эндпоинты:
- ✅ `GET /` - health check
- ✅ `POST /register` (deprecated)
- ✅ `POST /create_order` (deprecated)
- ✅ `POST /users/register` (новый)
- ✅ `POST /orders` (новый)

## 📚 Документация

Добавлены файлы:
- `REFACTORING.md` - подробное описание всех изменений
- `MERGE_GUIDE.md` - инструкции по merge и миграции
- `QUICK_MERGE.md` - быстрая шпаргалка
- `PR_DESCRIPTION.md` - этот файл

## ⚠️ Breaking Changes

**Нет breaking changes!** Все изменения обратно совместимы.

## 🔗 Связанные Issue

_(Если есть issue в GitHub, укажите здесь)_

## 👥 Reviewers

_(Добавьте ревьюеров, если работаете в команде)_

## 🏷️ Labels

`refactoring` `enhancement` `architecture` `clean-code`

---

## Чек-лист автора

- [x] Код соответствует style guide проекта
- [x] Все тесты проходят
- [x] Добавлена документация
- [x] Обратная совместимость сохранена
- [x] Нет breaking changes
- [x] README обновлен (если нужно)
- [x] CHANGELOG обновлен (если есть)

## Чек-лист ревьюера

- [ ] Код прочитан и понятен
- [ ] Архитектура решения логична
- [ ] Тесты покрывают основные сценарии
- [ ] Нет security issues
- [ ] Производительность не ухудшилась
- [ ] Документация адекватна

---

**Автор:** Claude Code (with human guidance)
**Ветка:** `strange-lederberg`
**Базовая ветка:** `main`
**Дата:** 2025-12-04
