# Инструкция по безопасной совместной работе с Git-репозиторием

## 1. Базовые правила работы с ветками

### Никогда не работайте напрямую в `main`/`master`

```bash
# ❌ ПЛОХО - работа в main
git checkout main
# делаете изменения...
git commit -m "changes"
git push

# ✅ ХОРОШО - работа в feature-ветке
git checkout -b feature/my-feature
# делаете изменения...
git commit -m "Add new feature"
git push origin feature/my-feature
```

### Проверяйте текущую ветку перед работой

```bash
# Всегда проверяйте на какой ветке вы находитесь
git branch
# или
git status

# Должно быть: On branch feature/your-feature
# НЕ должно быть: On branch main
```

---

## 2. Правила коммитов

### Проверяйте что добавляете ПЕРЕД коммитом

```bash
# ❌ ПЛОХО - слепое добавление всего
git add .
git commit -m "update"

# ✅ ХОРОШО - осознанное добавление
git status                    # Посмотрите что изменено
git diff                      # Посмотрите ЧТО именно изменилось
git add specific_file.py      # Добавьте конкретный файл
git commit -m "feat: Add user authentication"
```

### Используйте осмысленные сообщения коммитов

```bash
# ❌ ПЛОХО
git commit -m "fix"
git commit -m "update"
git commit -m "changes"

# ✅ ХОРОШО
git commit -m "fix: Resolve UTF-8 encoding issue in test script"
git commit -m "feat: Add user registration endpoint"
git commit -m "docs: Update API documentation"
```

**Формат:** `<type>: <краткое описание>`

Типы:
- `feat:` - новая функциональность
- `fix:` - исправление бага
- `docs:` - документация
- `refactor:` - рефакторинг кода
- `test:` - добавление тестов
- `chore:` - обновление зависимостей, конфигов

---

## 3. Синхронизация с удаленным репозиторием

### Перед началом работы - ВСЕГДА подтягивайте изменения

```bash
# Начало рабочего дня / новая задача
git checkout tender-germain
git pull origin tender-germain

# Теперь создайте feature-ветку от актуальной версии
git checkout -b feature/new-feature
```

### Перед push - подтяните последние изменения

```bash
# Перед отправкой своих изменений
git fetch origin
git rebase origin/tender-germain  # Или git merge origin/tender-germain

# Если возникли конфликты - разрешите их
# Затем push
git push origin feature/new-feature
```

---

## 4. Работа с изменениями (staging)

### Используйте правильные команды git add

```bash
# Добавить ТОЛЬКО измененные и удаленные файлы (БЕЗ новых)
git add -u

# Добавить ВСЕ изменения (включая новые файлы)
git add -A

# Добавить конкретный файл (РЕКОМЕНДУЕТСЯ)
git add path/to/specific_file.py

# Добавить по интерактивному режиму (выбираете что добавить)
git add -p
```

### Проверяйте staged изменения перед коммитом

```bash
# Посмотреть что в staging area
git diff --cached

# Убрать файл из staging (если добавили случайно)
git restore --staged unwanted_file.txt

# Отменить ВСЕ staged изменения
git restore --staged .
```

---

## 5. Отмена ошибок

### Если сделали неправильный коммит (ЕЩЕ НЕ сделали push)

```bash
# Отменить последний коммит, сохранив изменения
git reset --soft HEAD~1

# Отменить последний коммит, удалив изменения (ОПАСНО!)
git reset --hard HEAD~1

# Изменить сообщение последнего коммита
git commit --amend -m "Новое сообщение"
```

### Если уже сделали push неправильного коммита

```bash
# НЕ используйте force push в shared ветках!
# Вместо этого создайте новый коммит, отменяющий изменения
git revert HEAD
git push origin branch-name
```

### Если случайно изменили не тот файл

```bash
# Отменить изменения в конкретном файле
git restore path/to/file.txt

# Вернуть файл к версии из конкретного коммита
git checkout HEAD~1 -- path/to/file.txt
```

---

## 6. Работа с конфликтами при pull/merge

### Если git pull выдает ошибку о локальных изменениях

```bash
# Вариант 1: Сохранить изменения временно
git stash
git pull
git stash pop  # Применить изменения обратно

# Вариант 2: Закоммитить изменения
git add .
git commit -m "WIP: work in progress"
git pull

# Вариант 3: Отменить локальные изменения (ОПАСНО!)
git reset --hard HEAD
git pull
```

### Разрешение merge конфликтов

```bash
# После git pull с конфликтами
git status  # Посмотрите файлы с конфликтами

# Откройте файл, найдите маркеры:
# <<<<<<< HEAD
# ваши изменения
# =======
# изменения из remote
# >>>>>>> origin/branch

# Отредактируйте файл, удалив маркеры
# Затем:
git add resolved_file.txt
git commit -m "merge: Resolve conflicts in resolved_file.txt"
```

---

## 7. Проверка перед push в shared ветки

### Чек-лист перед push

```bash
# 1. Проверьте на какой ветке вы
git branch

# 2. Посмотрите что будете отправлять
git log origin/branch-name..HEAD --oneline

# 3. Убедитесь что тесты проходят
./scripts/test_api.sh
# или
pytest

# 4. Проверьте что нет лишних файлов
git status

# 5. Только после всех проверок
git push origin branch-name
```

---

## 8. Настройка Git для предотвращения проблем

### Глобальные настройки

```bash
# Настроить имя и email (обязательно!)
git config --global user.name "Ваше Имя"
git config --global user.email "your.email@example.com"

# Цветной вывод для удобства
git config --global color.ui auto

# Автоматическое определение переименований
git config --global diff.renames true

# Настроить редактор по умолчанию
git config --global core.editor "nano"  # или vim, code, etc.

# Игнорировать изменения прав файлов (для Linux/Mac)
git config --global core.fileMode false

# Настроить line endings (важно для кросс-платформенной работы)
# На Windows:
git config --global core.autocrlf true
# На Linux/Mac:
git config --global core.autocrlf input
```

### .gitignore для проекта

Создайте `.gitignore` в корне проекта:

```gitignore
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
.venv

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Секреты
.env
*.key
*.pem
credentials.json

# Временные файлы
*.log
*.tmp
*~
```

---

## 9. Workflow для совместной работы

### Рекомендуемый процесс

```bash
# 1. Начало работы над задачей
git checkout main
git pull origin main
git checkout -b feature/task-name

# 2. Работа над задачей
# ... делаете изменения ...
git add specific_files
git commit -m "feat: Add feature description"

# 3. Регулярная синхронизация с main
git fetch origin
git rebase origin/main  # Или merge, если rebase пугает

# 4. Перед финальным push
git fetch origin
git rebase origin/main
# Запустите тесты
./run_tests.sh

# 5. Push и создание PR
git push origin feature/task-name
# Создайте Pull Request на GitHub/GitLab

# 6. После мерджа PR - очистка
git checkout main
git pull origin main
git branch -d feature/task-name  # Удалить локальную ветку
```

---

## 10. SSH vs HTTPS для аутентификации

### Настройка SSH (рекомендуется)

```bash
# 1. Генерация SSH ключа
ssh-keygen -t ed25519 -C "your.email@example.com"

# 2. Добавить ключ в ssh-agent
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519

# 3. Скопировать публичный ключ
cat ~/.ssh/id_ed25519.pub
# Добавьте его в GitHub: Settings → SSH and GPG keys → New SSH key

# 4. Проверить соединение
ssh -T git@github.com

# 5. Изменить remote на SSH
git remote set-url origin git@github.com:username/repo.git
```

### Использование Personal Access Token (альтернатива)

```bash
# 1. Создайте токен на GitHub:
# Settings → Developer settings → Personal access tokens → Generate new token

# 2. При push используйте токен вместо пароля
# Username: ваш-github-username
# Password: вставьте-токен

# 3. Сохранить credentials
git config --global credential.helper cache  # На 15 минут
git config --global credential.helper store  # Навсегда (менее безопасно)
```

---

## 11. Полезные алиасы

Добавьте в `~/.gitconfig`:

```ini
[alias]
    st = status
    co = checkout
    br = branch
    ci = commit
    unstage = restore --staged
    last = log -1 HEAD
    visual = log --oneline --graph --all --decorate
    amend = commit --amend --no-edit
```

Использование:

```bash
git st        # вместо git status
git co main   # вместо git checkout main
git visual    # красивый граф коммитов
```

---

## 12. Частые ошибки и их решения

| Проблема | Решение |
|----------|---------|
| "Permission denied" при push | Настройте SSH или используйте Personal Access Token |
| "Please commit your changes or stash them" | `git stash` → `git pull` → `git stash pop` |
| Случайно закоммитили в main | `git reset --soft HEAD~1` → переключитесь на feature-ветку → `git commit` |
| UTF-8 символы ломают скрипты | Используйте только ASCII в shell скриптах |
| Line endings конфликтуют (CRLF/LF) | Настройте `core.autocrlf` правильно для вашей ОС |
| Забыли переключиться на ветку | `git log` (найдите хеш коммита) → `git cherry-pick <hash>` в правильной ветке |

---

## 13. Правила code review (для Pull Requests)

### Перед созданием PR

- ✅ Убедитесь что все тесты проходят
- ✅ Проверьте что код соответствует стилю проекта
- ✅ Добавьте описание в PR: что делает, зачем, как тестировали
- ✅ Убедитесь что ветка актуальная (rebase от main)

### Не мержите PR если

- ❌ Есть failing tests
- ❌ Есть unresolved комментарии от reviewers
- ❌ Ветка устарела (нужен rebase)
- ❌ Добавлены секреты/credentials в код

---

## 14. Безопасность

### НИКОГДА не коммитьте

- 🔒 API ключи, токены, пароли
- 🔒 `.env` файлы с секретами
- 🔒 SSH приватные ключи
- 🔒 Сертификаты
- 🔒 Database credentials

### Если случайно закоммитили секрет

```bash
# 1. Немедленно смените секрет/токен!

# 2. Удалите коммит (если еще не сделали push)
git reset --hard HEAD~1

# 3. Если уже сделали push - используйте git-filter-branch или BFG Repo-Cleaner
# И ОБЯЗАТЕЛЬНО смените скомпрометированный секрет!
```

---

## Резюме: Золотые правила

1. **Всегда работайте в feature-ветках**, не в main
2. **`git status` и `git diff` перед каждым коммитом**
3. **`git pull` перед началом работы и перед push**
4. **Осмысленные commit messages**
5. **Не используйте `git add .` бездумно**
6. **Проверяйте тесты перед push**
7. **Никогда не коммитьте секреты**
8. **Используйте SSH для аутентификации**
9. **При ошибке - `git stash`, не паникуйте**
10. **В сомнениях - спросите коллег, не делайте force push**

---

**Сохраните эту инструкцию и следуйте ей!** Это сэкономит вам часы на разруливание проблем. 🚀
