# Инструкция для нового разработчика

Это краткое руководство для быстрого старта работы над проектом NMservices.

---

## 1. Первоначальная настройка

### Клонирование репозитория

```bash
# HTTPS (потребует токена при push)
git clone https://github.com/imchrm/NMservices.git
cd NMservices

# Или SSH (рекомендуется, настройка ниже)
git clone git@github.com:imchrm/NMservices.git
cd NMservices
```

### Настройка Git

```bash
# Обязательно настройте имя и email
git config --global user.name "Ваше Имя"
git config --global user.email "your.email@example.com"

# Настройка line endings (важно для кросс-платформенной работы)
# На Windows:
git config --global core.autocrlf true

# На Linux/Mac:
git config --global core.autocrlf input
```

### Настройка SSH (рекомендуется)

```bash
# 1. Генерация SSH ключа
ssh-keygen -t ed25519 -C "your.email@example.com"

# 2. Добавить ключ в ssh-agent
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519

# 3. Скопировать публичный ключ и добавить в GitHub
cat ~/.ssh/id_ed25519.pub
# GitHub: Settings → SSH and GPG keys → New SSH key

# 4. Изменить remote на SSH (если клонировали через HTTPS)
git remote set-url origin git@github.com:imchrm/NMservices.git
```

---

## 2. Структура веток в проекте

```
main (защищенная ветка, только через PR)
  │
  └── tender-germain (рабочая ветка команды)
         │
         ├── feature/payment-system (разработчик 1)
         ├── feature/user-profile (разработчик 2)
         └── fix/api-timeout (разработчик 3)
```

### Важно:

- **main** - защищенная ветка, изменения только через Pull Request
- **tender-germain** - основная рабочая ветка команды
- **feature/*** - ваши feature-ветки создаются НАПРЯМУЮ от `tender-germain`
- ❌ НЕ создавайте личные промежуточные ветки (типа `your-name-branch`)

---

## 3. Ежедневный workflow

### Начало работы над новой задачей

```bash
# 1. Переключитесь на рабочую ветку
git checkout tender-germain

# 2. Подтяните последние изменения
git pull origin tender-germain

# 3. Создайте feature-ветку для вашей задачи
git checkout -b feature/my-task-name

# Примеры хороших названий:
# feature/add-payment-api
# feature/user-authentication
# fix/registration-validation
# refactor/database-queries
```

### Работа над задачей

```bash
# 1. Делайте изменения в коде

# 2. ВСЕГДА проверяйте что изменили
git status
git diff

# 3. Добавляйте файлы осознанно (НЕ git add .)
git add path/to/specific_file.py
git add another_file.py

# 4. Коммит с осмысленным сообщением
git commit -m "feat: Add payment processing endpoint"

# Формат commit message: <type>: <описание>
# Типы:
# - feat: новая функциональность
# - fix: исправление бага
# - docs: документация
# - refactor: рефакторинг
# - test: добавление тестов
# - chore: обновление зависимостей, конфигов
```

### Синхронизация с командой (делайте регулярно!)

```bash
# Перед push ВСЕГДА синхронизируйтесь
git fetch origin
git rebase origin/tender-germain

# Если возникли конфликты:
# 1. Откройте файлы с конфликтами (git покажет их)
# 2. Найдите маркеры <<<<<<< и >>>>>>>
# 3. Разрешите конфликт вручную
# 4. Добавьте разрешенные файлы
git add resolved_file.py
git rebase --continue
```

### Отправка изменений и создание Pull Request

```bash
# 1. Push вашей ветки
git push origin feature/my-task-name

# 2. Создайте Pull Request на GitHub
# Перейдите на github.com/imchrm/NMservices
# GitHub автоматически предложит создать PR

# 3. В описании PR укажите:
# - Что делает ваш код
# - Как тестировали
# - Ссылки на связанные issues (если есть)

# 4. Дождитесь code review
# 5. Внесите правки если потребуется
# 6. После одобрения PR будет смержен в tender-germain
```

---

## 4. Тестирование API

Перед созданием PR **обязательно** запустите тесты:

### На Linux/Mac:

```bash
# Сделать скрипт исполняемым (один раз)
chmod +x ./scripts/test_api.sh

# Запустить тесты
./scripts/test_api.sh

# С custom параметрами
HOST=94.158.50.119 PORT=9800 API_KEY=troxivasine23 ./scripts/test_api.sh

# Verbose режим
./scripts/test_api.sh -v
```

### На Windows PowerShell:

```powershell
# Запустить тесты
.\scripts\test_api.ps1

# С custom параметрами
.\scripts\test_api.ps1 -HostName 94.158.50.119 -Port 9800 -ApiKey "troxivasine23"

# Verbose режим
.\scripts\test_api.ps1 -Verbose
```

**Все 6 тестов должны пройти успешно!**

---

## 5. Частые ошибки и решения

### "Permission denied" при push

**Проблема:** Не настроен SSH или токен

**Решение:** Настройте SSH ключи (см. раздел 1) или используйте Personal Access Token

---

### "Please commit your changes or stash them"

**Проблема:** Есть незакоммиченные изменения при попытке pull

**Решение:**

```bash
# Вариант 1: Временно спрятать изменения
git stash
git pull origin tender-germain
git stash pop

# Вариант 2: Закоммитить изменения
git add .
git commit -m "WIP: work in progress"
git pull origin tender-germain
```

---

### Случайно закоммитили в tender-germain вместо feature-ветки

**Проблема:** Забыли создать feature-ветку

**Решение (если еще НЕ сделали push):**

```bash
# 1. Отменить коммит, сохранив изменения
git reset --soft HEAD~1

# 2. Создать правильную ветку
git checkout -b feature/my-task

# 3. Закоммитить снова
git commit -m "feat: Your changes"
```

---

### Merge конфликты

**Проблема:** При rebase/merge возникли конфликты

**Решение:**

```bash
# 1. Посмотрите какие файлы конфликтуют
git status

# 2. Откройте файлы и найдите маркеры:
# <<<<<<< HEAD
# ваши изменения
# =======
# изменения из remote
# >>>>>>> origin/tender-germain

# 3. Отредактируйте файл, оставив нужный код
# 4. Удалите маркеры <<<<<<< ======= >>>>>>>
# 5. Добавьте разрешенный файл
git add resolved_file.py

# 6. Продолжите rebase
git rebase --continue

# Если запутались - отмените rebase
git rebase --abort
```

---

## 6. Работа в tmux (для удаленных серверов)

Если вы работаете на удаленном сервере через SSH:

```bash
# Создать новую сессию
tmux new -s dev

# Отключиться от сессии (не завершая процессы)
# Нажмите: Ctrl+B, затем D

# Подключиться к существующей сессии
tmux attach -t dev

# Список сессий
tmux ls

# Перезапустить shell внутри tmux
exec bash
```

**Преимущество:** Даже если SSH соединение оборвется, ваша работа в tmux продолжится.

---

## 7. Checklist перед созданием Pull Request

- [ ] Код работает локально
- [ ] Все тесты проходят (`./scripts/test_api.sh` или `.\scripts\test_api.ps1`)
- [ ] Синхронизировались с `tender-germain` (`git rebase origin/tender-germain`)
- [ ] Commit messages осмысленные
- [ ] Не добавили секреты/пароли/API ключи в код
- [ ] Не добавили лишние файлы (проверили `git status`)
- [ ] Ветка называется понятно (`feature/...` или `fix/...`)

---

## 8. Полезные команды

```bash
# Посмотреть статус (какие файлы изменены)
git status

# Посмотреть изменения в файлах
git diff

# Посмотреть staged изменения
git diff --cached

# История коммитов
git log --oneline

# Граф веток
git log --oneline --graph --all --decorate

# Посмотреть удаленные ветки
git branch -r

# Удалить локальную ветку после мерджа
git branch -d feature/my-task

# Отменить изменения в файле (до коммита)
git restore path/to/file.py

# Убрать файл из staging
git restore --staged path/to/file.py
```

---

## 9. Где получить помощь

1. **Документация Git:** `docs/GIT_WORKFLOW_GUIDE.md` (детальное руководство)
2. **Проблемы с проектом:** Спросите в командном чате
3. **Git помощь:** `git help <command>` (например, `git help commit`)
4. **API тесты:** `./scripts/test_api.sh --help`

---

## Резюме: Быстрый старт за 5 шагов

```bash
# 1. Синхронизация
git checkout tender-germain
git pull origin tender-germain

# 2. Создать feature-ветку
git checkout -b feature/my-task

# 3. Работа
# ... делаете изменения ...
git add specific_file.py
git commit -m "feat: Add new feature"

# 4. Синхронизация перед push
git fetch origin
git rebase origin/tender-germain

# 5. Push и PR
git push origin feature/my-task
# Создайте Pull Request на GitHub
```

---

**Главное правило:** Всегда работайте в feature-ветках, никогда не коммитьте напрямую в `tender-germain` или `main`!

Удачи в разработке! 🚀
