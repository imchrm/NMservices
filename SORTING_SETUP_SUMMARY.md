# Sorting Support - Краткая инструкция

**Дата:** 2026-01-25
**Статус:** ✅ Реализовано

---

## ✅ Что сделано

Добавлена поддержка динамической сортировки для Admin API endpoints:
- `GET /admin/users` - сортировка пользователей
- `GET /admin/orders` - сортировка заказов

---

## 📝 Измененные файлы

1. **src/nms/api/admin/users.py** - добавлены параметры `sort_by` и `order`
2. **src/nms/api/admin/orders.py** - добавлены параметры `sort_by` и `order`
3. **scripts/test_sorting.sh** - скрипт для тестирования сортировки
4. **CHANGELOG_SORTING.md** - детальная документация

---

## 🚀 Как использовать

### Users

**Доступные поля для сортировки:**
- `id` (по умолчанию)
- `phone_number`
- `created_at`
- `updated_at`

**Примеры:**
```bash
# Сортировка по ID (по умолчанию, ascending)
GET /admin/users

# Сортировка по дате создания (descending) - новые первые
GET /admin/users?sort_by=created_at&order=desc

# Сортировка по номеру телефона
GET /admin/users?sort_by=phone_number&order=asc

# С пагинацией
GET /admin/users?skip=0&limit=25&sort_by=created_at&order=desc
```

---

### Orders

**Доступные поля для сортировки:**
- `id`
- `user_id`
- `status`
- `total_amount`
- `created_at` (по умолчанию)
- `updated_at`

**Примеры:**
```bash
# Сортировка по дате (по умолчанию, descending) - новые первые
GET /admin/orders

# Сортировка по сумме (descending) - от большей к меньшей
GET /admin/orders?sort_by=total_amount&order=desc

# Сортировка по статусу с фильтром
GET /admin/orders?status_filter=pending&sort_by=created_at&order=desc

# Сортировка по user_id
GET /admin/orders?sort_by=user_id&order=asc
```

---

## 🧪 Проверка работы

### Вариант 1: Тестовый скрипт (рекомендуется)

```bash
./scripts/test_sorting.sh http://localhost:8000 admin_secret
```

Скрипт проверит:
- ✅ Сортировку users по всем полям
- ✅ Сортировку orders по всем полям
- ✅ Валидацию (неправильные параметры отклоняются с 422)

### Вариант 2: Curl вручную

```bash
# Пользователи по дате (новые первые)
curl -H "X-Admin-Key: admin_secret" \
  "http://localhost:8000/admin/users?sort_by=created_at&order=desc" | jq

# Заказы по сумме (от большей к меньшей)
curl -H "X-Admin-Key: admin_secret" \
  "http://localhost:8000/admin/orders?sort_by=total_amount&order=desc" | jq
```

### Вариант 3: Swagger UI

1. Открыть http://localhost:8000/docs
2. Найти `GET /admin/users` или `GET /admin/orders`
3. Нажать "Try it out"
4. Выбрать `sort_by` и `order` из dropdown
5. Нажать "Execute"

---

## 📊 Параметры запроса

### Общие параметры (для обоих endpoints):

| Параметр | Тип | Значения | По умолчанию | Описание |
|----------|-----|----------|--------------|----------|
| `skip` | integer | 0+ | 0 | Пропустить N записей |
| `limit` | integer | 1-1000 | 100 | Вернуть N записей |
| `order` | string | `asc`, `desc` | varies | Порядок сортировки |

### Users специфичные:

| Параметр | Значения | По умолчанию |
|----------|----------|--------------|
| `sort_by` | `id`, `phone_number`, `created_at`, `updated_at` | `id` |

### Orders специфичные:

| Параметр | Значения | По умолчанию |
|----------|----------|--------------|
| `sort_by` | `id`, `user_id`, `status`, `total_amount`, `created_at`, `updated_at` | `created_at` |
| `status_filter` | string (e.g., `pending`, `completed`) | null |

---

## 💡 React Admin интеграция

С добавленной сортировкой React Admin будет работать автоматически:

```typescript
// В dataProvider.ts
getList: (resource, params) => {
  const { page, perPage } = params.pagination;
  const { field, order } = params.sort;  // ✅ Теперь работает!

  const url = `${API_URL}/${resource}?` +
    `skip=${(page - 1) * perPage}&` +
    `limit=${perPage}&` +
    `sort_by=${field}&` +
    `order=${order.toLowerCase()}`;

  return httpClient(url).then(({ json }) => ({
    data: json.users || json.orders || [],
    total: json.total || 0,
  }));
}
```

**Теперь можно кликать на заголовки столбцов для сортировки!** 🎉

---

## 🔒 Безопасность

### ✅ Защита от SQL Injection

Используется **whitelist подход**:

```python
# Разрешены только эти поля
sort_columns = {
    "id": User.id,
    "phone_number": User.phone_number,
    "created_at": User.created_at,
    "updated_at": User.updated_at,
}
```

Попытка сортировки по другим полям вернет **422 Unprocessable Entity**.

### ✅ Валидация типов

- `sort_by` - только разрешенные значения (Literal type)
- `order` - только `asc` или `desc`
- Невалидные значения автоматически отклоняются FastAPI

---

## ⚡ Производительность

### Индексы БД

Для быстрой сортировки нужны индексы:

```sql
-- Users
CREATE INDEX idx_users_created_at ON users(created_at);
CREATE INDEX idx_users_phone_number ON users(phone_number);

-- Orders
CREATE INDEX idx_orders_created_at ON orders(created_at);
CREATE INDEX idx_orders_user_id ON orders(user_id);
CREATE INDEX idx_orders_status ON orders(status);
CREATE INDEX idx_orders_total_amount ON orders(total_amount);
```

**Большинство уже созданы** через SQLAlchemy (`index=True` в моделях).

---

## 🐛 Troubleshooting

### Проблема: Сортировка не работает

**Проверьте:**
1. Backend обновлен и перезапущен
2. Параметры передаются правильно:
   ```bash
   # ✅ Правильно
   ?sort_by=created_at&order=desc

   # ❌ Неправильно
   ?sortBy=created_at&Order=DESC
   ```

### Проблема: 422 Unprocessable Entity

**Причина:** Невалидное поле или порядок

**Решение:**
- Используйте только разрешенные поля (см. таблицы выше)
- `order` должен быть `asc` или `desc` (lowercase)

---

## 📋 Чеклист

- [x] Сортировка добавлена в users endpoint
- [x] Сортировка добавлена в orders endpoint
- [x] Валидация параметров работает
- [x] Тестовый скрипт создан
- [x] Документация обновлена
- [ ] Backend перезапущен (если нужно)
- [ ] Сортировка протестирована

---

## 🎯 Следующие шаги

После добавления сортировки:

1. ✅ **Backend полностью готов** для Admin Panel
2. ✅ **CORS настроен** (предыдущий коммит)
3. ✅ **Сортировка работает**
4. 🚀 **Можно создавать React Admin frontend**

---

## 📚 Дополнительные ресурсы

- `CHANGELOG_SORTING.md` - Детальная документация
- `docs/ARCHITECTURE_QUESTIONS_ANSWERS.md` - Вопрос 1 (Сортировка)
- `ADMIN_API.md` - Полная документация Admin API
- [FastAPI Query Parameters](https://fastapi.tiangolo.com/tutorial/query-params/)

---

**Готов к frontend разработке!** 🚀
